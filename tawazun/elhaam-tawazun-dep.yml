apiVersion: apps/v1
kind: Deployment
metadata:
  name: tawazun-app
  labels:
    app: tawazun
  namespace: tawazun
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tawazun
  template:
    metadata:
      labels:
        app: tawazun
    spec:
      containers:
      - image: abdullahkimrigh/tawazun-sda-hackathon:0.1
        name: tawazun-app
        ports:
        - containerPort: 80
          name: tawazun
        livenessProbe:
          httpGet:
            path: /probes
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /probes
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10
        startupProbe:
          httpGet:
            path: /probes
            port: 80
          failureThreshold: 30
          periodSeconds: 10